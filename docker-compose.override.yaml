#services:
#    tests:
#        image: "eduki/php-vouchers-assignment:latest"
#        command: [ "-s", "basic" ]
#        environment:
#            APP_BASE_URL: ""
#
# docker run -it --rm --network=host --env APP_BASE_URL=http://api.example-project.work:8080 eduki/php-vouchers-assignment:latest -s basic
